name: egnn-transformer
channels:
  - pytorch
  - anaconda
  - conda-forge
  - defaults
  - pytorch
  # - nvidia # maybe
dependencies:
  - pytorch
  - pytorch-cuda
  - tqdm
  - torch_geometric
  - lightning
  - jax # need to adjust versions [cuda] or [cpu]
  - flax