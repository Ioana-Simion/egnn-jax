name: egnn-transformer
channels:
  - pytorch
  - conda-forge
  - anaconda
  - defaults
dependencies:
  - pytorch
  - tqdm
  - lightning
  - rdkit
  - pip
  - pip:
    - "jax[cpu]"
    - torch_geometric
    - flax
